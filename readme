C6C - a c-like language compiler

1. Compile C6C:
	make c6c
2. Use C6C to compile test programs into assembly code:
	./c6c < tests/[filename] > test.as
3. Run the assembly using nas:
	./nas test.as
4. Sample game application "tic-tac-toe":
   The file ttt.sc is the source code, to compile it into assembly and play the game, follow the instruction above.